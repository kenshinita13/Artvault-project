# SQL Scripts

This folder contains Supabase SQL setup, schema, policy, and feature scripts used during ArtVault development.

Run only the scripts needed for your target database environment. The React/Vite web app does not import these files at runtime.
